Τίτλος Συστήματος
π.χ. Σύστημα Διαχείρισης Αιτήσεων Αδειών Υπαλλήλων

1. Περιγραφή
Σύντομη περιγραφή του σεναρίου που υλοποιεί η ομάδα:

Ποιος είναι ο βασικός σκοπός του συστήματος;
Ποιοι είναι οι βασικοί χρήστες (ρόλοι);
Ποιο use case (ή ποιο υποσύνολο) υλοποιούμε από άκρη σε άκρη;
Παράδειγμα:

Η εφαρμογή υλοποιεί ένα απλό σύστημα στο οποίο ένας υπάλληλος υποβάλλει αίτηση άδειας, ο προϊστάμενος την εγκρίνει ή την απορρίπτει και το σύστημα αποθηκεύει το ιστορικό αιτήσεων.

2. Μέλη Ομάδας
Ονοματεπώνυμο 1 – ΑΜ: XXXXXXX
Ονοματεπώνυμο 2 – ΑΜ: XXXXXXX
Ονοματεπώνυμο 3 – ΑΜ: XXXXXXX
3. Τεχνολογίες
Γλώσσα: Python 3.10+
Εξωτερικές βιβλιοθήκες: (αν χρησιμοποιείτε κάτι εκτός standard library, γράψτε το εδώ)
π.χ. tabulate για όμορφα tables (προαιρετικό)
4. Πώς τρέχω το πρόγραμμα
Clone του repository:

git clone <URL ΤΟΥ REPOSITORY>
cd <φάκελος του project>
Εκτέλεση:

python main.py
Αν υπάρχει κάποια ειδική απαίτηση (π.χ. Python virtualenv, requirements.txt), περιγράψτε τη εδώ.

5. Δομή του κώδικα
Σύντομη περιγραφή των αρχείων:

e2e_lab_system/    # Ρίζα του project
.
├── main.py         # Είσοδος του προγράμματος / μενού / διάλογος με χρήστη
├── models.py       # Κλάσεις τομέα (π.χ. User, Request, Document, κ.λπ.)
├── services.py     # Λογική συστήματος / συναρτήσεις χειρισμού σεναρίων
└── diagrams/
    ├── class_diagram.png
    └── sequence_diagram.png
Περιγράψτε με 1–2 προτάσεις τι κάνει το κάθε αρχείο:

main.py:

περιέχει το μενού,
διαβάζει input από τον χρήστη,
καλεί τις συναρτήσεις από services.py.
models.py:

περιέχει τις βασικές κλάσεις του συστήματος (οντότητες).
services.py:

περιέχει συναρτήσεις που υλοποιούν τα βήματα του use case (δημιουργία/αναζήτηση/ενημέρωση αντικειμένων).
6. UML Διαγράμματα
Στον φάκελο diagrams/ περιλαμβάνονται:

class_diagram.png – class diagram των βασικών κλάσεων που υλοποιούνται.

sequence_diagram.png – sequence diagram για το βασικό use case.

Μπορείτε να προσθέσετε και άλλα (π.χ. .puml, .pdf) αν θέλετε.

7. Παράδειγμα Εκτέλεσης
Εδώ βάλτε ένα ρεαλιστικό σενάριο χρήσης, με είσοδο/έξοδο από το terminal.

Παράδειγμα:

Καλωσήρθατε στο σύστημα ΧΧΧ

1. Δημιουργία νέας αίτησης
2. Προβολή αιτήσεων
0. Έξοδος
Επιλογή: 1

Όνομα υπαλλήλου: Maria Papadopoulou
Ημερομηνία έναρξης άδειας (YYYY-MM-DD): 2025-03-10
Ημερομηνία λήξης άδειας (YYYY-MM-DD): 2025-03-15
Αιτιολογία: Προσωπικοί λόγοι

Η αίτηση καταχωρήθηκε με κωδικό: REQ-0001

1. Δημιουργία νέας αίτησης
2. Προβολή αιτήσεων
0. Έξοδος
Επιλογή: 2

Λίστα αιτήσεων:
REQ-0001 - Maria Papadopoulou (2025-03-10 έως 2025-03-15) [ΕΚΚΡΕΜΕΙ]

Επιλογή: 0
Αντίο!
(Μπορείτε να κάνετε copy-paste από πραγματικό run του προγράμματός σας.)

8. Γνωστά Περιοριστικά Σημεία / Μελλοντική Επέκταση
Προαιρετικό, αλλά καλό αν θέλετε να δείξετε ότι σκέφτεστε σαν developers:

Τι ΔΕΝ υποστηρίζει ακόμα το σύστημα (π.χ. δεν αποθηκεύει σε αρχείο, δεν έχει έλεγχο για invalid ημερομηνίες).
Τι θα προσθέτατε σε επόμενη έκδοση.
Παράδειγμα:

Προς το παρόν τα δεδομένα χάνονται όταν κλείσει το πρόγραμμα (δεν υπάρχει αποθήκευση σε αρχείο).
Δεν γίνεται έλεγχος αν η ημερομηνία λήξης είναι πριν από την ημερομηνία έναρξης.
Σε επόμενη έκδοση θα μπορούσαμε να προσθέσουμε αποθήκευση σε JSON/CSV και απλό έλεγχο ημερομηνιών.